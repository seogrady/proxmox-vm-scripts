name = "gitea"
version = "1.0.0"
kind = "resource-module"

[implementation]
resource = "resources/gitea/resource.toml"
service = "services/gitea/service.toml"
default_target = "lxc"
fallback_target = "vm"

[hooks]
bootstrap = "scripts/provision-gitea.sh"
validate = "scripts/validate-gitea.sh"
