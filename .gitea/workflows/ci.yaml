name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    runs-on: vmctl
    steps:
      - name: Checkout
        uses: https://gitea.com/actions/checkout@v4

      - name: Run tests
        run: |
          cargo test --locked
