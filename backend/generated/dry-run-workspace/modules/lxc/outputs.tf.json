{
  "output": {
    "name": {
      "description": "Rendered vmctl lxc resource name.",
      "value": "${var.resource.name}"
    },
    "vmid": {
      "description": "Rendered vmctl lxc resource VMID.",
      "value": "${try(var.resource.vmid, null)}"
    }
  }
}