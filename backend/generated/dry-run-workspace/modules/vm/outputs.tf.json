{
  "output": {
    "name": {
      "description": "Rendered vmctl vm resource name.",
      "value": "${var.resource.name}"
    },
    "vmid": {
      "description": "Rendered vmctl vm resource VMID.",
      "value": "${try(var.resource.vmid, null)}"
    }
  }
}